{% extends 'base.html.twig' %}

{% set virtual_title = entity.description[:50]  %}

{% block title %}
    {{ virtual_title }}
    {{ parent() }}
{% endblock %}

{% block opengraph_title %}
    <meta property="og:title" content="{{ virtual_title }} - {{ app_name }}" />
{% endblock %}

{% block body %}
    <div class="col-sm-8">
        {{ include ('partials/_entity-card.html.twig') }}
    </div>
    {{ render(controller('App\\Controller\\EmbedController::sidebar')) }}
{% endblock %}
