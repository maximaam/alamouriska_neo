{% extends 'email.html.twig' %}

{% block header %}
    Bonjour {{ receiver}} !
{% endblock %}

{% block body %}
    <p> tu as un message de: 
    <a href="{{ url('app_frontend_member_profile', {'pseudo': sender }) }}" target="_blank">{{ sender }}</a>
    </p>
    <fieldset>
        <legend>Message:</legend>
        <p style="padding:10px; background-color:#ddd">{{ message }}</p>
    </fieldset>
    <br>
{% endblock %}
