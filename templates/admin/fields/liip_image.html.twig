{% if field.value %}
    {% set filter_name = field.customOptions.get('liip_filter') %}
    <img src="{{ asset(field.formattedValue | imagine_filter(filter_name)) }}" alt="Display Image" width="64" height="64">
{% endif %}