<form method="post" action="{{ path('app_wall_delete', {'id': wall.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ wall.id) }}">
    <button class="btn">Delete</button>
</form>
